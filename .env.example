# Copy to .env to override the token committed in app.json's `extra`.
# EXPO_PUBLIC_ vars are inlined into the client bundle by Metro, which is
# correct here: a basemap token is sent with every tile request and is public
# by construction. Do not put anything genuinely secret behind this prefix.
EXPO_PUBLIC_CARTO_API_KEY=
